---
import Diamond from "./Diamond.astro";

const year = new Date().getFullYear();

const navLinks = [
  { n: "01", label: "Mission",  href: "#mission" },
  { n: "02", label: "Services", href: "#services" },
  { n: "03", label: "Brands",   href: "#dna" },
  { n: "04", label: "Archive",  href: "#moments" },
  { n: "05", label: "Team",     href: "#team" },
  { n: "06", label: "Contact",  href: "#contact" },
];

const serviceLinks = [
  "Content Strategy",
  "Product Strategy",
  "Brand Strategy",
  "Podcast Roadmap",
  "Platform Development",
  "Business Consulting",
];

const contacts = [
  { role: "Founder", name: "Tim Sabean",  email: "TimSabean@me.com",   phone: "+1 646-812-3300", phoneHref: "tel:+16468123300" },
  { role: "Partner", name: "Gil Edwards", email: "GilEdwards@outlook.com", phone: "+1 267-254-6991", phoneHref: "tel:+12672546991" },
];
---

<footer class="relative overflow-hidden bg-ink text-bone">
  <!-- CTA callout row -->
  <section class="relative border-t border-[rgba(212,175,55,0.2)]">
    <div aria-hidden="true" class="pointer-events-none absolute -left-32 top-1/2 h-[400px] w-[400px] -translate-y-1/2 rounded-full bg-[radial-gradient(circle,rgba(212,175,55,0.09)_0%,transparent_60%)] blur-3xl"></div>

    <div class="mx-auto flex max-w-[1600px] flex-col items-center gap-10 px-6 py-16 text-center md:flex-row md:items-center md:justify-between md:gap-14 md:px-10 md:py-20 md:text-left">
      <div class="reveal">
        <div class="flex items-center justify-center gap-3 md:justify-start">
          <Diamond size={14} class="text-[color:var(--gold)]" stroke={1.2} />
          <span
            class="text-[10px] uppercase tracking-[0.5em] text-[color:var(--gold)]"
            style="font-family: var(--font-mono);"
          >
            Visioneering · 2026
          </span>
        </div>
        <h2 class="mt-6 font-display text-[clamp(2.25rem,4.4vw,3.75rem)] font-light leading-none tracking-[-0.02em]">
          Let's build
          <span class="italic text-shine">something unforgettable.</span>
        </h2>
      </div>

      <div class="flex flex-wrap items-center justify-center gap-4 reveal reveal-delay-1 md:justify-start">
        <a href="#contact" class="btn-gold">
          Start a Conversation
          <svg viewBox="0 0 20 20" class="h-3.5 w-3.5" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M4 10h12M10 4l6 6-6 6"/></svg>
        </a>
        <a href="mailto:TimSabean@me.com" class="btn-ghost">Email Direct</a>
      </div>
    </div>
  </section>

  <!-- Main grid -->
  <section class="border-t border-[rgba(212,175,55,0.12)]">
    <div class="mx-auto grid max-w-[1600px] grid-cols-1 gap-x-8 gap-y-14 px-6 py-16 text-center md:grid-cols-12 md:gap-10 md:px-10 md:py-20 md:text-left">
      <!-- Brand -->
      <div class="flex flex-col items-center md:col-span-4 md:items-start">
        <a href="#top" class="group inline-flex items-center" aria-label="The Sabean Group — Home">
          <img
            src="/images/logos/sabean-group.png"
            alt="The Sabean Group"
            width="1600"
            height="700"
            loading="lazy"
            decoding="async"
            class="block h-14 w-auto md:h-20 transition-transform duration-500 group-hover:scale-[1.03] drop-shadow-[0_4px_20px_rgba(212,175,75,0.22)]"
          />
        </a>

        <p class="mt-7 max-w-xs text-[0.92rem] leading-relaxed text-bone/70">
          Content, Creator, Brand and Media Platform Strategy — engineered for 360° growth, multi-vertical revenue and audience monetization.
        </p>

        <address class="mt-7 text-[0.62rem] not-italic uppercase tracking-[0.28em] text-bone-muted leading-loose">
          <span class="text-gold-400">Based in</span><br />
          <span class="text-bone">New York, NY</span><br />
          Remote · US
        </address>
      </div>

      <!-- Navigate -->
      <div class="md:col-span-2">
        <p class="mb-5 flex items-center justify-center gap-2 text-[0.6rem] uppercase tracking-[0.32em] text-gold-400 md:justify-start" style="font-family: var(--font-mono);">
          Navigate
          <span class="hidden h-px flex-1 bg-[rgba(212,175,55,0.25)] md:inline-block"></span>
        </p>
        <ul class="space-y-3 text-sm">
          {navLinks.map((l) => (
            <li>
              <a href={l.href} class="group inline-flex items-baseline gap-3 text-bone-muted transition-colors hover:text-bone">
                <span class="text-[0.6rem] text-gold-400/70 transition-colors group-hover:text-gold-300">({l.n})</span>
                <span class="relative">
                  {l.label}
                  <span class="absolute -bottom-1 left-0 h-px w-0 bg-gold-300 transition-all duration-500 group-hover:w-full"></span>
                </span>
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Services -->
      <div class="md:col-span-3">
        <p class="mb-5 flex items-center justify-center gap-2 text-[0.6rem] uppercase tracking-[0.32em] text-gold-400 md:justify-start" style="font-family: var(--font-mono);">
          Services
          <span class="hidden h-px flex-1 bg-[rgba(212,175,55,0.25)] md:inline-block"></span>
        </p>
        <ul class="space-y-3 text-sm">
          {serviceLinks.map((s, i) => (
            <li>
              <a href="#services" class="group inline-flex items-baseline gap-3 text-bone-muted transition-colors hover:text-bone">
                <span class="text-[0.6rem] text-gold-400/70 transition-colors group-hover:text-gold-300">
                  0{i + 1}
                </span>
                <span>{s}</span>
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Direct Contact -->
      <div class="md:col-span-3">
        <p class="mb-5 flex items-center justify-center gap-2 text-[0.6rem] uppercase tracking-[0.32em] text-gold-400 md:justify-start" style="font-family: var(--font-mono);">
          Direct Contact
          <span class="hidden h-px flex-1 bg-[rgba(212,175,55,0.25)] md:inline-block"></span>
        </p>
        <ul class="space-y-5">
          {contacts.map((c) => (
            <li class="flex flex-col items-center md:items-start">
              <p class="text-[0.58rem] uppercase tracking-[0.3em] text-gold-400">{c.role}</p>
              <p class="mt-1 font-display text-lg font-light text-bone">{c.name}</p>
              <a href={`mailto:${c.email}`} class="mt-1 inline-flex items-center gap-2 text-[0.85rem] text-bone/80 transition-colors hover:text-gold-200">
                <span class="text-gold-300">→</span>
                {c.email}
              </a>
              <a href={c.phoneHref} class="inline-flex items-center gap-2 text-[0.72rem] uppercase tracking-[0.22em] text-bone-muted transition-colors hover:text-gold-200">
                <span class="text-gold-300">→</span>
                {c.phone}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>
  </section>

  <!-- Legal row -->
  <section class="border-t border-[rgba(212,175,55,0.12)]">
    <div class="mx-auto flex max-w-[1600px] flex-col items-center gap-4 px-6 py-6 text-center text-[0.6rem] uppercase tracking-[0.28em] text-bone-muted md:flex-row md:items-center md:justify-between md:px-10 md:text-left">
      <p>© {year} The Sabean Group, LLC · All rights reserved</p>

      <div class="flex flex-wrap items-center justify-center gap-4 md:justify-end">
        <span class="hidden md:inline">Engagements at custom retainer levels</span>
        <span class="hidden h-px w-8 bg-[rgba(212,175,55,0.3)] md:inline-block"></span>
        <a href="#contact" class="hover:text-gold-200 transition-colors">Privacy</a>
        <a href="#contact" class="hover:text-gold-200 transition-colors">Terms</a>
      </div>
    </div>
  </section>

</footer>
